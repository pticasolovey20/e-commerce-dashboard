import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
	root: {
		display: "flex",
		alignItems: "center",
		paddingRight: "37px",
	},
	icon: {
		marginRight: "45px !important",
	},
});
